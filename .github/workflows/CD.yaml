name: CD (.NET Core → Azure WebApp)

on:
  push:
    branches: [ main ]  # Solo despliega en main o release

jobs:
  deploy:
    name: Publish & Deploy to Azure
    runs-on: windows-latest
    environment: production

    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9.x
        uses: actions/setup-dotnet@v3
        with:
          dotnet-version: '9.x'

      - name: dotnet publish
        run: |
          dotnet publish Frock-backend/Frock-backend.csproj --configuration Release --output publish

      # Agrega aquí tus pasos de despliegue (por ejemplo, Azure WebApp)
      # - name: Deploy to Azure WebApp
      #   uses: azure/webapps-deploy@v2
      #   with:
      #     app-name: ${{ secrets.AZURE_WEBAPP_NAME }}
      #     publish-profile: ${{ secrets.AZURE_WEBAPP_PUBLISH_PROFILE }}
      #     package: ./publish
